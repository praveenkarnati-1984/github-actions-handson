name: 05 - 02 - Event Filters and Activity Types
on:
    pull_request:
        types:
            - closed
        branches:
        - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR closed AND base branch is main"